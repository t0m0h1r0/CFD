# Import key components for spatial discretization
from .base import SpatialDiscretizationBase

# Explicitly define the submodules to be imported
from . import operators

# Optional: Create version info
__version__ = "0.1.0"
